export enum SortOrder {
  DEFAULT = "default",
  ALPHABET = "alphabet",
  REVERSE_ALPHABET = "reverse-alphabet",
}

export enum FetchStatus {
  Idle = "idle",
  Loading = "loading",
  Succeeded = "succeeded",
  Failed = "failed",
}
